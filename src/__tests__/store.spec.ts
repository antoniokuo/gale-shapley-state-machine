// src/__tests__/store.spec.ts
import { describe, expect, it } from 'vitest'

describe('CI Pipeline Sanity Check', () => {
  it('verifies mathematical truth', () => {
    expect(1 + 1).toBe(2)
  })
})
